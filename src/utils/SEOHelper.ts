export const generateMetadata = (title: string, desc: string) => {
    return {
        title: `${title} | Ethereal Couture`,
        description: desc,
        robots: "index, follow"
    };
};